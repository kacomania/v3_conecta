import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Core Brand Colors (Fallback)
  static const Color primary = Color(0xFF003B73);
  static const Color secondary = Color(0xFF005B9F);

  // Backgrounds & Surfaces
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color backgroundGrey = Color(0xFFF0F4F8);
  static const Color blueLight = Color(0xFFE8EEF4);

  // Borders & Accents
  static const Color greyLight = Color(0xFFD9D9D9);
  static const Color greyDark = Color(0xFF555555);

  // Semantic Colors
  static const Color error = Color(0xFFD32F2F);
  static const Color success = Color(0xFF388E3C);
}
